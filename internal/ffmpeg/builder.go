// Package ffmpeg builds and runs ffmpeg commands with a shared skeleton and retry logic.
package ffmpeg

// TODO: Implement shared command skeleton + encode/remux codec-specific args.
// -probesize, -analyzeduration, -max_muxing_queue_size, map, -dn, etc.
