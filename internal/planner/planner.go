// Package planner decides per-file action: encode, remux, or skip, and builds FilePlan.
package planner

// TODO: Implement decision matrix, edge-safe HEVC check, BuildPlan(Config, ProbeResult) -> FilePlan.
// Action (Encode/Remux/Skip), VideoCodec, AudioPlan, SubtitlePlan, etc.
